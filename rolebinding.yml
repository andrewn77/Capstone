       kind: RoleBinding

       apiVersion: rbac.authorization.k8s.io/v1

       metadata:

          name: roleCapstone
          namespace: default
       subjects:

       - kind: ServiceAccount

         name: user1

         namespace: default

       roleRef:

         kind: Role

         name: user1-role

         apiGroup: ""

